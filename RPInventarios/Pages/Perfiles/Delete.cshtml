@page "{id:int?}"
@model RPInventarios.Pages.Perfil.DeleteModel

@{
    ViewData["Title"] = "¿Desea eliminar el perfil con nombre " + Model.Perfil.Nombre + " ?";
}

<h4>@ViewData["Title"]</h4>
<hr />
<div>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Perfil.Nombre)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Perfil.Nombre)
        </dd>
    </dl>

    <form method="post">
        <input type="hidden" asp-for="Perfil.Id" />
        <partial name="Acciones/_Delete" />
    </form>
</div>
